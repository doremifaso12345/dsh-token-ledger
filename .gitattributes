# 统一行尾为 LF。
# 本机 git init 时继承了 core.autocrlf=true，会在检入检出时把行尾改成 CRLF，
# 对跨平台协作的公开仓库是噪音。这里显式钉死 LF，覆盖 autocrlf。
* text=auto eol=lf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.zip binary

# 明确的文本类型（防止 auto 误判）
*.js   text eol=lf
*.mjs  text eol=lf
*.json text eol=lf
*.yml  text eol=lf
*.md   text eol=lf
